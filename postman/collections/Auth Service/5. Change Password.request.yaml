name: Change Password
method: POST
url: "{{base_url}}/auth/change-password/1"
headers:
  Authorization: "Bearer {{token}}"
  Content-Type: application/json
body:
  mode: raw
  raw: |
    {
      "newPassword": "newPassword123"
    }
