$kind: http-request
url: "{{base_url}}/auth/change-password/{{id}}"
method: POST
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
        "newPassword": "newpassword123"
    }
order: 7000
