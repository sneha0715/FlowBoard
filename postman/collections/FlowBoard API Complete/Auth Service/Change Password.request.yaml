$kind: http-request
url: "{{auth_url}}/auth/change-password/{{user_id}}"
method: POST
headers:
  Content-Type: application/json
  Authorization: Bearer {{token}}
body:
  type: text
  content: |-
    {
      "newPassword": "newpassword123"
    }
order: 7000
