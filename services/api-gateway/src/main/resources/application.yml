server:
  port: ${GATEWAY_PORT:8080}

spring:
  application:
    name: api-gateway
  main:
    banner-mode: off
    web-application-type: reactive
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowed-origins:
              - http://localhost:3000
              - http://localhost:4200
              - http://localhost:5173
              - http://localhost:5174
            allowed-methods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
              - PATCH
            allowed-headers: "*"
            allow-credentials: true
            max-age: 3600
      routes:
        # ── PUBLIC Auth routes (no AuthenticationFilter) ──────────────────────
        - id: auth-service-public
          uri: lb://auth-service
          predicates:
            - Path=/api/v1/auth/login,/api/v1/auth/register
          filters:
            - StripPrefix=2

        # ── SECURED Auth routes ────────────────────────────────────────────────
        - id: auth-service-secured
          uri: lb://auth-service
          predicates:
            - Path=/api/v1/auth/**
          filters:
            - name: AuthenticationFilter
            - StripPrefix=2

        # ── Workspace Service ──────────────────────────────────────────────────
        - id: workspace-service
          uri: lb://workspace-service
          predicates:
            - Path=/api/v1/workspaces/**
          filters:
            - name: AuthenticationFilter
            - StripPrefix=2

        # ── Board Service ──────────────────────────────────────────────────────
        - id: board-service
          uri: lb://board-service
          predicates:
            - Path=/api/v1/boards/**
          filters:
            - name: AuthenticationFilter
            - StripPrefix=2

        # ── Column Service ─────────────────────────────────────────────────────
        - id: column-service
          uri: lb://column-service
          predicates:
            - Path=/api/v1/columns/**
          filters:
            - name: AuthenticationFilter
            - StripPrefix=2

        # ── Card Service ───────────────────────────────────────────────────────
        - id: card-service
          uri: lb://card-service
          predicates:
            - Path=/api/v1/cards/**
          filters:
            - name: AuthenticationFilter
            - StripPrefix=2

        # ── Comment Service ────────────────────────────────────────────────────
        - id: comment-service
          uri: lb://comment-service
          predicates:
            - Path=/api/v1/comments/**
          filters:
            - name: AuthenticationFilter
            - StripPrefix=2


        # ── Attachment Service (part of comment-service) ───────────────────────
        - id: attachment-service
          uri: lb://comment-service
          predicates:
            - Path=/api/v1/attachments/**
          filters:
            - name: AuthenticationFilter
            - StripPrefix=2

        # ── Checklist Service ──────────────────────────────────────────────────
        - id: checklist-service
          uri: lb://checklist-service
          predicates:
            - Path=/api/v1/checklists/**
          filters:
            - name: AuthenticationFilter
            - StripPrefix=2


        # ── Label Service (part of checklist-service) ─────────────────────────
        - id: label-service
          uri: lb://checklist-service
          predicates:
            - Path=/api/v1/labels/**
          filters:
            - name: AuthenticationFilter
            - StripPrefix=2

        # ── Notification Service ───────────────────────────────────────────────
        - id: notification-service
          uri: lb://notification-service
          predicates:
            - Path=/api/v1/notifications/**
          filters:
            - name: AuthenticationFilter
            - StripPrefix=2
        
        # ── API Docs routing for Swagger UI ────────────────────────────────────
        - id: auth-service-api-docs
          uri: lb://auth-service
          predicates:
            - Path=/v3/api-docs/auth-service
          filters:
            - SetPath=/v3/api-docs

        - id: workspace-service-api-docs
          uri: lb://workspace-service
          predicates:
            - Path=/v3/api-docs/workspace-service
          filters:
            - SetPath=/v3/api-docs

        - id: board-service-api-docs
          uri: lb://board-service
          predicates:
            - Path=/v3/api-docs/board-service
          filters:
            - SetPath=/v3/api-docs

        - id: column-service-api-docs
          uri: lb://column-service
          predicates:
            - Path=/v3/api-docs/column-service
          filters:
            - SetPath=/v3/api-docs

        - id: card-service-api-docs
          uri: lb://card-service
          predicates:
            - Path=/v3/api-docs/card-service
          filters:
            - SetPath=/v3/api-docs

        - id: comment-service-api-docs
          uri: lb://comment-service
          predicates:
            - Path=/v3/api-docs/comment-service
          filters:
            - SetPath=/v3/api-docs

        - id: checklist-service-api-docs
          uri: lb://checklist-service
          predicates:
            - Path=/v3/api-docs/checklist-service
          filters:
            - SetPath=/v3/api-docs

        - id: notification-service-api-docs
          uri: lb://notification-service
          predicates:
            - Path=/v3/api-docs/notification-service
          filters:
            - SetPath=/v3/api-docs

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka/}
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: false
    hostname: localhost

jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    urls:
      - name: auth-service
        url: /v3/api-docs/auth-service
      - name: workspace-service
        url: /v3/api-docs/workspace-service
      - name: board-service
        url: /v3/api-docs/board-service
      - name: column-service
        url: /v3/api-docs/column-service
      - name: card-service
        url: /v3/api-docs/card-service
      - name: comment-service
        url: /v3/api-docs/comment-service
      - name: checklist-service
        url: /v3/api-docs/checklist-service
      - name: notification-service
        url: /v3/api-docs/notification-service
